const os = require("os");

console.log(os.homedir());  // diretório principal da máquina
console.log(os.cpus());     // cpus de um servidor
console.log(os.freemem());  // memória ram livre na máquina
console.log(os.type());     // qual sistema operacional esta rodando na máquina
